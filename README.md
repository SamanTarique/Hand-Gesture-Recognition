# Hand-Gesture-Recognition
This project is a CNN-based Hand Gesture Recognition system that classifies different hand gestures using image data. It includes data preprocessing, EDA, model training, evaluation, and prediction on random images using a trained deep learning model.
